**Add your own guidelines here**
<!--

# General Python Guidelines
* **Estandar de Codigo:** Sigue estrictamente PEP 8 para el estilo de codigo.
* **Tipado:** Usa "Type Hints" (ej. `def suma(a: int, b: int) -> int:`) para que el codigo sea mas facil de leer.
* **Nomenclatura:** Variables y funciones en `snake_case`, clases en `PascalCase`.
* **Comentarios:** Explica la logica compleja en español, pero manten el codigo (variables y funciones) en ingles.

# App UI & Experience
* **Scroll Obligatorio:** Todas las vistas principales deben ser "scrollables". Si el contenido excede el alto de la pantalla, el usuario debe poder deslizar hacia abajo sin que se rompa la interfaz.
* **Contenedores:** Envuelve siempre el contenido en un contenedor con `scroll=True` (o el equivalente según la libreria que estemos usando).
* **Responsividad:** Asegurate de que los elementos se centren y se ajusten al ancho de la pantalla del celular (simulacion movil).

# Estructura del Proyecto
* **Modularidad:** No pongas todo el codigo en un solo archivo. Separa la logica de la interfaz (UI) de la logica de datos (Backend).
* **Manejo de Errores:** Usa bloques `try-except` en funciones criticas para evitar que la app se cierre inesperadamente.
-->
